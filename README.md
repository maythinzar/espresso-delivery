# Espresso Delivery

## Deployment

### Web

Create all devices template code
```shell
flutter create .
```

Build the web app
```shell
flutter build web 
```

The output files are located in `./build/web`